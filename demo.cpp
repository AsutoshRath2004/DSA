#include <bits/stdc++.h> // includes every library in C++
using namespace std; // using the namespace std allows you to prevent writing std:: before every standard library function

int main() {
    string x, y;
    getline(cin, x); // getline is used to read a line of text from the input
    cin >> y;
    std::cout << x << "\n";
    cout << y ;
} 